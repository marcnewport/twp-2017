# backend:
#     name:    netlify-git-api
#     url:     http://localhost:8080

backend:
    name: github
    repo: marcnewport/twp-2017 # Path to your Github repository
    branch: master # Branch to update (master by default)

media_folder:  "assets/uploads" # Folder where user uploaded files should go

collections: # A list of collections the CMS should be able to edit
  - name: "work" # Used in routes, ie.: /admin/collections/:slug/edit
    label: "Work" # Used in the UI, ie.: "New Post"
    folder: "_works" # The path to the folder where the documents are stored
    sort: "date:desc" # Default is title:asc
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}"
    fields: # The fields each document in this collection have
      - {label: "Layout", name: "layout", widget: "hidden", default: "work"}
      - {label: "Title", name: "title", widget: "string", tagname: "h1"}
      - {label: "Subtitle", name: "subtitle", widget: "markdown"}
      - {label: "Introduction", name: "introduction", widget: "markdown"}
      - {label: "Deliverables", name: "deliverables", widget: "list"}
      - {label: "Website address", name: "website", widget: "string"}
      - {label: "Category", name: "category", widget: "string"}
      - {label: "Main image", name: "main-image", widget: "image"}
      - {label: "Main text", name: "main-text", widget: "markdown"}
      - {label: "Testimonial", name: "testimonial-text", widget: "markdown"}
      - {label: "Testimonial author", name: "testimonial-author", widget: "string"}
      - {label: "Testimonial position", name: "testimonial-position", widget: "string"}
      - {label: "Testimonial company", name: "testimonial-company", widget: "string"}
      - {label: "Desktop screenshot 1", name: "desktop-1", widget: "image"}
      - {label: "Desktop screenshot 2", name: "desktop-2", widget: "image"}
      - {label: "Desktop screenshot text", name: "desktop-text", widget: "markdown"}
      - {label: "Mobile screenshot 1", name: "mobile-1", widget: "image"}
      - {label: "Mobile screenshot 2", name: "mobile-2", widget: "image"}
      - {label: "Mobile screenshot 3", name: "mobile-3", widget: "image"}
      - {label: "Mobile screenshot text", name: "mobile-text", widget: "markdown"}
    meta: # Meta data fields. Just like fields, but without any preview element
      - {label: "Publish Date", name: "date", widget: "datetime", format: "YYYY-MM-DD hh:mm:ss"}

  - name: "news" # Used in routes, ie.: /admin/collections/:slug/edit
    label: "News" # Used in the UI, ie.: "New Post"
    folder: "_news" # The path to the folder where the documents are stored
    sort: "date:desc" # Default is title:asc
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}"
    fields: # The fields each document in this collection have
      - {label: "Layout", name: "layout", widget: "hidden", default: "post"}
      - {label: "Title", name: "title", widget: "string", tagname: "h1"}
      - {label: "Main image", name: "main-image", widget: "image"}
      - {label: "Body", name: "body", widget: "markdown"}
      - {label: "Category", name: "category", widget: "string"}
      - {label: "Author", name: "author", widget: "string"}
    meta: # Meta data fields. Just like fields, but without any preview element
      - {label: "Publish Date", name: "date", widget: "datetime", format: "YYYY-MM-DD hh:mm:ss"}
